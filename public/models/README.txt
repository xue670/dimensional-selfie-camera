Place a bundled default GLB model here as:

default.glb

The app will load it from:

/models/default.glb

This is intended as a temporary mobile-testing fallback when browser file picking for GLB is unreliable.
